version: '3.8'

services:
  stream-redirector:
    build: .
    container_name: stream-redirector
    ports:
      - "6095:5000"  # Host port 6095 maps to container port 5000
    environment:
      - FLASK_ENV=production
    restart: unless-stopped